//작곡 참여 View
import React from 'react';
import PlayingDetailPresenter from './PlayingDetailPresenter';

const PlayingDetailContainer = props => {
  return <PlayingDetailPresenter {...props} />;
};

export default PlayingDetailContainer;
